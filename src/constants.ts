import { ElementProperties, PhysicalState } from './types';

import defaultElements from './defaultelements.json';

export const BASE_ELEMENTS: ElementProperties[] = defaultElements as ElementProperties[];
